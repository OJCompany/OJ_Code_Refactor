# 프로젝트 코드 컨벤션

- 모든 인터페이스 이름은 `I` 접두사를 붙인다 (예: `IOrderItem`, `ICustomer`)
- 타입 별칭(type alias)은 사용하지 않는다. 반드시 `interface`를 사용한다
- 함수 반환 타입은 항상 명시한다
- `unknown` 대신 명시적 인터페이스를 정의해서 사용한다
- 에러 메시지는 한국어로 작성한다
